/* ARREGLOS */

/* SOLO ES RECOMENDABLE HACERLO DE ESTA MANERA SI SE LE VA A DAR UN TAMAÑO FIJO */
// const arreglo = new Array(100);

/* ASI ES COMO SE DEBE USAR LOS ARREGLOS */
const arreglo = [1, 2, 3, 4];
/* LOS ARREGLOS SON OBJETOS EN JS TAMBIEN */

/* NO ES RECOMENDABLE UTILIZAR EL PUSH, YA QUE ESTE MODIFICA EL OBJETO PRINCIPAL */
/* CUANDO QUERAMOS INSERTAR ALGO, LO HAREMOS UTILIZANDO EL OPERADOR SPREAD */
// arreglo.push(1);
// let arreglo2 = arreglo; -> COPIA LA REFERENCIA DEL ARREGLO, MODIFICA AMBOS
// arreglo2.push(5);

/* PARA COPIAR UN ARREGLO HACEMOS LO SIGUIENTE, A PARTE DE AGREGAMOS UN DATO ADICIONAL = 5*/
let arreglo2 = [...arreglo, 5];

/* MAP -> EL METODO MAP ESPERA UNA FUNCION(CALLBACK) */
/* LA FUNCION SE VA A EJECUTAR POR CADA UNO DE LOS ELEMENTOS QUE ESTE DENTRO DEL ARREGLO2 */
/* EL ARGUMENTO QUE RECIBE VIENE SIENDO EL NUMERO (ES COMO UN CICLO) */
const arreglo3 = arreglo2.map( (numero) => {
    return numero*2;
});

console.log(arreglo);
console.log(arreglo2);
console.log(arreglo3);